# Model Artifact Overview

This file explains what model artifacts are expected to exist in the project and how they should be interpreted.

Typical artifacts include:

- checkpoints,
- metadata files,
- model configuration snapshots,
- evaluation summaries,
- training logs.

A future engineer should be able to tell which files are active, which are historical, and which are simply experiment-related outputs.
