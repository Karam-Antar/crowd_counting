# Glossary

This document defines the most common terms used in the project.

- density map: continuous spatial representation of crowd distribution
- count estimate: sum of the density field or related predicted total
- preprocessing contract: the expected sequence of data transforms and normalization
- validation split: dataset subset used for model selection or evaluation
- MLflow run: a logged experiment instance including config, metrics, and artifacts
- checkpoint: saved model state from a training run
