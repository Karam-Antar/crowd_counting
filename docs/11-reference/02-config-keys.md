# Config Keys

This file documents the most important configuration keys used across the project.

Examples include:

- dataset path values,
- model output paths,
- random seed settings,
- label scaling parameters,
- MLflow log directory settings,
- training and validation split options.

This is helpful when debugging experiments, tracing run behavior, or comparing a current run with a historical one.
