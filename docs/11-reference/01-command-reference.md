# Command Reference

This file should contain the main operational commands used for common tasks in the project.

Examples include:

- dataset conversion commands,
- training commands,
- tuning commands,
- artifact inspection commands,
- local validation commands.

The exact list should be maintained as workflows evolve, but the purpose is always the same: make the project easier to repeat and easier to debug.
