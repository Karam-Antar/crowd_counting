# Evaluation Metrics

Evaluation needs to capture more than just the final count. A model may produce a reasonable count while generating a poor density field.

This project therefore uses metrics and inspection methods that reflect the spatial quality of the output as well as the aggregate count.
