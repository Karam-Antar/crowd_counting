# Density Map Intuition

A density map converts discrete point annotations into a continuous field. This is useful because it preserves the spatial structure of the crowd rather than reducing the scene to a single count value.

The sum of the density map is interpreted as the total crowd count. This gives the model both a local and global understanding of the scene.
