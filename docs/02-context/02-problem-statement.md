# Problem Statement

Crowd counting is the task of estimating how many people are present in a scene when dense overlap, perspective distortion, and occlusion make per-person detection unreliable.

Because counting requires both localization and quantity estimation, the project models crowd density as a continuous spatial field instead of relying only on a single scalar count prediction.
