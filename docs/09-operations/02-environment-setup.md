# Environment Setup

This project depends on a consistent Python environment, training dependencies, and data path configuration.

## Setup checklist

- install the repository requirements,
- confirm the project root and data path configuration,
- verify that the dataset is present in the expected location,
- ensure the local scripts can access the environment variables required by the project.

A good environment setup is critical because some project assumptions are stored outside the code in local configuration.
