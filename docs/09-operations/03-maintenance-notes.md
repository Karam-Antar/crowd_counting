# Maintenance Notes

This project evolves over time, and maintenance notes are important for keeping the repo coherent.

Good maintenance notes include:

- refactoring decisions,
- config defaults that are easy to alter accidentally,
- cleanup steps that should not be repeated unnecessarily,
- historical assumptions that remain relevant even when the code changes.

These notes are especially important in a project with many MLflow runs and multiple experimental code paths.
