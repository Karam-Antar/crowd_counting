# Common Errors

This page should summarize the recurring problems encountered in this project.

Examples include:

- mismatched image and target pair counts,
- invalid dataset structure,
- incorrect labels or density-map format,
- preprocessing drift across runs,
- incorrectly compared experiments that changed both code and data at once.

A written list of common errors is useful because these problems are easy to forget once the project becomes more mature.
