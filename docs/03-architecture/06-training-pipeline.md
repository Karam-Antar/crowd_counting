# Training Pipeline

The training pipeline includes data loading, preprocessing, augmentation, validation, and artifact logging.

This part of the project is especially important because small changes in preprocessing can significantly alter the training signal. The project therefore treats the current data contract as a documented operational guarantee rather than an undocumented assumption.
