# Dataset Cleanup Notes

This project has already gone through cleanup decisions that should be preserved in writing.

Examples include:

- adding negative samples,
- removing mislabeled examples,
- fixing dataset mismatches caused by download variants,
- aligning folder structure across train and validation data.

These changes matter because they affect both training quality and comparability between runs.
