# Production Checklist

Before a model is treated as production-ready, confirm:

- the dataset and preprocessing contract are stable,
- the validation metrics are reliable,
- artifact provenance is recorded,
- serving logic matches the training assumptions,
- the known limitations are documented.

This checklist helps prevent a model from being promoted simply because it achieved a good score in a single run.
