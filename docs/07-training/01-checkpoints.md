# Checkpoints

Checkpoints are the project’s main model artifacts. They capture the state of the model at a specific training point and should be interpreted with awareness of the data and config in effect when they were produced.

This file should explain where checkpoints are stored, how to identify a useful one, and when it is appropriate to reuse a historical checkpoint for evaluation or debugging.
