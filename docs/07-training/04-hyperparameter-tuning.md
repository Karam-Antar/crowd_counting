# Hyperparameter Tuning

This project uses tuning and experimentation to explore training settings, preprocessing choices, and architecture-related tradeoffs.

The tuning process is important because many of the project’s differences across runs come from configuration changes rather than from a dramatic model redesign. This makes good tuning records and MLflow metadata essential.
