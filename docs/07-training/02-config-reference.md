# Config Reference

This file documents the main configuration values used by the project.

Common values to track include:

- seed values,
- label scaling,
- dataset root configuration,
- model checkpoint path,
- experiment log path,
- training and validation settings.

The current project depends on a small but critical set of values that affect reproducibility and training behavior.
