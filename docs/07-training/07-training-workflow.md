# Training Workflow

The training workflow includes data loading, preprocessing, model optimization, validation, and artifact logging.

This file should remain aligned with the actual scripts and config used to train the project model, because the project relies on a documented training contract rather than informal assumptions.
