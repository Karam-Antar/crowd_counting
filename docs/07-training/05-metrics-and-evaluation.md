# Metrics and Evaluation

The project’s evaluation strategy should reflect both the count estimate and the quality of the predicted density map.

This is especially important because crowd counting is a spatial regression problem. A model that predicts the right total count might still have poor scene-level density structure if the training or preprocessing assumptions are inconsistent.
