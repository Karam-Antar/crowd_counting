# Branch and Experiment Summary

This document explains how major experiment branches, scripts, and MLflow runs relate to one another.

It is especially useful in a project where different training runs were based on different assumptions about data cleanup, model architecture, or preprocessing. Without a summary, the historical code can appear arbitrary rather than intentionally iterative.
